# 点歌系统服务端
这是一个基于 Node.js 的点歌系统服务端，用于处理点歌请求和管理点歌队列。
## 功能
- 支持用户发送点歌请求
- 管理点歌队列，包括添加、删除、查询等操作
- 支持多用户并发点歌
- 提供 API 接口，方便前端调用
## 安装依赖
```bash
pnpm install
```
## 运行服务
```bash
pnpm start
```
## 配置环境变量
- `PORT`：服务端口，默认 3000
- `IP`：服务 IP，默认 127.0.0.1
